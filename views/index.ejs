<!DOCTYPE html>
<html lang="en">
  <%- include('header') -%>
  <body>
    <%- include('nav') -%>
    <div class="display content">
      <% if(blogs.length > 0) { %> 
        <% blogs.forEach(blog=> { %>
      <div class="post">
        <a href="/blogs/<%= blog._id %>">
          <h3><%=blog.title %></h3>
          <p><%=blog.snippet %></p>
        </a>
      </div>
      <% }) %> <% } else { %>
      <h3>There is no post available...</h3>
      <% } %>
    </div>
    <%- include('footer') -%>
  </body>
</html>
